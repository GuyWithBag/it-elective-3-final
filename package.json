{
  "name": "it-elective-3-final-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace wrapper for running client and server together",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}


